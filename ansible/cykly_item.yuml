---

- hosts: all
  become: true
  task
  - name: Install packeges
    ansible.builtin.dnf:
      name: "{{ item }}"
      state: installed
    with_items:
        - net-tools
        - nfs-utils
        - nmap
